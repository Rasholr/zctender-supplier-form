# ZCTender — Supplier intake form

Static single-page form (`index.html`) that writes new suppliers into Supabase.
Hosted on Cloudflare Pages / Netlify — every push to `main` auto-deploys to the live URL.

Edit `index.html`, commit, push → live in ~30s. No manual upload.
